stages:
    - test

before_script:
  - python3 --version

test-csa-lab3:
    stage: test
    image:
        name: python-tools
        entrypoint: [""]
    script:
        - pip install pytest-golden
        - python3-coverage run -m pytest --verbose

