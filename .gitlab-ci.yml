lab3-example:
  stage: test
  image:
    name: python-tools
    entrypoint: [""]
  script:
    - echo "Running unit tests..."
    - pip3 install --no-cache-dir -r requirements.txt
    - pip install pytest
    - pytest

